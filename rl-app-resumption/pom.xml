<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.uhope</groupId>
    <artifactId>rl-app-resumption</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <description>rl-app-resumption project for Spring Boot</description>

    <parent>
        <groupId>com.uhope</groupId>
        <artifactId>rl-parent</artifactId>
        <version>1.0.1</version>
    </parent>
    <properties>
        <common.core.version>0.0.1-SNAPSHOT</common.core.version>
        <uip.base.service.client.version>0.0.1-SNAPSHOT</uip.base.service.client.version>
        <rl.app.component.service.client.version>0.0.1-SNAPSHOT</rl.app.component.service.client.version>
        <rl-supervise.service.client.version>0.0.1-SNAPSHOT</rl-supervise.service.client.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <!--MySQL JDBC驱动-->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.pagehelper</groupId>
            <artifactId>pagehelper</artifactId>
        </dependency>
        <!--阿里 Druid Spring Boot Starter依赖-->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>druid-spring-boot-starter</artifactId>
        </dependency>
        <!--阿里 FastJson依赖-->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>fastjson</artifactId>
        </dependency>
        <!--注册中心-->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-eureka</artifactId>
        </dependency>
        <!--引入feign-->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-feign</artifactId>
        </dependency>
        <!--引入测试包-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.uhope</groupId>
            <artifactId>common-core</artifactId>
            <version>${common.core.version}</version>
        </dependency>
        <!--常用库依赖-->
        <dependency>
            <groupId>commons-codec</groupId>
            <artifactId>commons-codec</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
        <!-- Data注解 -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.16.10</version>
        </dependency>

        <!-- 基础服务客户端 -->
        <dependency>
            <groupId>com.uhope.uip</groupId>
            <artifactId>uip-base-service-client</artifactId>
            <version>${uip.base.service.client.version}</version>
        </dependency>
        <!-- 部门管理和行政区域的通用平台基础服务客户端 -->
        <dependency>
            <groupId>com.uhope.rl</groupId>
            <artifactId>rl-app-basicdata-service-client</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
        <!-- 河长制3.0部件模块 -->
        <dependency>
            <groupId>com.uhope.rl</groupId>
            <artifactId>rl-app-component-service-client</artifactId>
            <version>${rl.app.component.service.client.version}</version>
        </dependency>
        <!-- 督办单下发管理服务 -->
        <dependency>
            <groupId>com.uhope.rl</groupId>
            <artifactId>rl-supervise-service-client</artifactId>
        </dependency>
        <!-- uip通用平台河湖库服务客户端 -->
        <dependency>
            <groupId>com.uhope.rl</groupId>
            <artifactId>rl-watersource-service-client</artifactId>
            <version>0.0.1-SNAPSHOT</version>
        </dependency>
        <!-- Redis工具 -->
        <dependency>
            <groupId>com.uhope.uip</groupId>
            <artifactId>redis-client</artifactId>
            <version>1.0.3</version>
        </dependency>
        <!-- junit单元测试 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>5.2.0</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${springCloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${springboot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


</project>
