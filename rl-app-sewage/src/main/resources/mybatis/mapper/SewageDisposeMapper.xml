<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uhope.template.mapper.SewageDisposeMapper">

    <select id="selectList" resultType="com.uhope.template.domain.SewageDispose">
        select * from sewage_dispose
        <where>
            <if test="issue != null ">
                and issue = #{issue}
            </if>
            <if test="status != null">
                AND status=#{status}
            </if>
            <if test="createUser != null">
                AND createUser like CONCAT('%',#{createUser},'%')
            </if>
            <if test="num !=null">
                and status !=#{num}
            </if>
        </where>
    </select>

    <!-- 查询市河长办的账号-->
    <select id="selectSHZB" resultType="java.lang.String">
        select sm_user.ID from sm_user INNER JOIN
        (select sur.USER_ID from sm_role sr INNER JOIN sm_user_role sur on sr.id=sur.ROLE_ID where sr.id='fca74e7d677111e7968ef01fafcf3a37'
        )s on sm_user.id =s.USER_ID where sm_user.region_id='120100000000';
    </select>

    <!--查询排监处的账号-->
    <select id="selectSHBJ" resultType="java.lang.String">
        select sm_user.ID from sm_user INNER JOIN
        (select sur.USER_ID from sm_role sr INNER JOIN sm_user_role sur on sr.id=sur.ROLE_ID where sr.id='e43c7b781d754deaa08f328a860e2194'
        )s on sm_user.id =s.USER_ID
    </select>
</mapper>